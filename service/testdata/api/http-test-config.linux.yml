paths:
  /about:
    get:
      plugins:
        - &weosPlugin
          filename: ../service/testdata/plugins/test.so
          config:
            MYSQL_HOST: localhost
            MYSQL_USER: root
            MYSQL_PASSWORD: root
